
### Generate python compiled files
Allow you to generate the bytecode version of a python file
```bash 
python -m py_compile prog.py
```

### Disassemble python files
Allow you to see the bytecode of a python file in a human-readable format
```bash
python -m dis prog.py
```